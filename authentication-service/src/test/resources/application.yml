server:
  port: 8081

spring:
  application:
    name: authentication-service
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  cloud:
    consul:
      enabled: false
    discovery:
      enabled: false

jwt:
  secret: TestSecretKeyForJWTTokenGeneration2024InstagramTestApp
  expiration: 86400000
